FROM node:20-alpine

WORKDIR /app

# Copy everything
COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# Don't prune - keep all dependencies
ENV NODE_ENV=production
EXPOSE 3000

CMD ["node", "dist/server/index.js"]
